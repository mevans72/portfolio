page('/', controller.home);
page('/about', MikeData.localData, controller.about);
page('/life', controller.life);
page('/projects', controller.projects);
page('/gh', controller.githubProjects);

page();
